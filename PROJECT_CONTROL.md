---
type: project-control
status: authoritative-remote-summary
updated: 2026-09-19
active_branch: agent/content-inventory-phase-1-2
---

# کنترل پروژهٔ SSOC

این صفحه تنها نقطهٔ شروع برای فهم وضعیت پروژه است. اگر یک فایل یا نقشه با آن ناسازگار بود، باید ناسازگاری ثبت و حل شود؛ نه اینکه بی‌صدا مسیر تازه‌ای ساخته شود.

## ۱. هدف پروژه

پژوهش و ساختِ مرحله‌ایِ یک هستهٔ محدود، شفاف و آزمون‌پذیر برای بررسیِ بعضی اجزای «فهم»؛ با الهام از آرشیو خام، اما بدون ادعای ساختِ فهم عمومی یا هوش انسانی.

هدف این مرحله «ساختن هوش کامل» نیست.

## ۲. نقطهٔ شروع

- آرشیو خام: ۱۰ نوت‌بوک Jupyter و یک فایل Python.
- پرسش‌های خام: فهم، عقل، فطرت، آگاهی، منطق، بازتاب، حرکت و نسبت آن‌ها با هوش مصنوعی.
- منبع اولیه: `main` و Snapshot `archive/original-2026-08-11`.

فایل‌های خام باید با Jupyter یا VS Code خوانده و اجرا شوند؛ Obsidian ابزار اجرای نوت‌بوک نیست.

## ۳. وضعیت رسمیِ تأییدشده روی GitHub

| مورد | وضعیت |
|---|---|
| فهرست‌برداری آرشیو و نقشهٔ مفهومی اولیه | ثبت‌شده |
| هستهٔ ۰٫۱: تشخیص تناقض با موضوع، زمان و زمینه | ثبت‌شده |
| specification، کد، CLI، API و آزمون‌های ۰٫۱ | ثبت‌شده |
| نتیجهٔ آزمایش ۰٫۱ | ۱۲/۱۲ در برابر ۸/۱۲ برای خط پایه؛ فقط جهان کنترل‌شده |
| فهم عمومی، یادگیری قاعده، فطرت یا خودآگاهی | ثبت و اثبات نشده |
| نسخهٔ ۰٫۲ و ۰٫۳ | در GitHub فعلی دیده نمی‌شوند؛ باید بازیابی یا نبودنشان قطعی شود |

## ۴. کارِ الآن

**بازیابی و تطبیق وضعیت واقعی.**

پیش از هر قابلیت جدید:

1. بررسی شود آیا Commitها و فایل‌های ۰٫۲/۰٫۳ در مک محلی وجود دارند یا نه.
2. اگر وجود دارند، SHA، فایل‌ها و آزمونشان ثبت شود.
3. فقط پس از روشن‌شدن این وضعیت، یک مسیر بعدی انتخاب شود.
4. تا آن زمان نسخهٔ جدید، شاخهٔ جدید یا ادعای جدید ساخته نشود.

## ۵. معیار پایان این مرحله

این مرحله وقتی تمام است که:

- وضعیت محلی و GitHub دربارهٔ ۰٫۲/۰٫۳ یکسان و قابل‌اثبات باشد.
- یک شاخهٔ کاریِ مشخص داشته باشیم.
- فقط یک قدم بعد با ورودی، خروجی، معیار موفقیت و معیار شکست تعریف شده باشد.

## ۶. نقش ابزارها

| ابزار | نقش |
|---|---|
| Jupyter / VS Code | بازکردن، خواندن، اجرا و اصلاح نوت‌بوک‌ها و کد |
| GitHub / Git | منبع نسخه، Commit، PR و بازیابی |
| Obsidian | فقط دفتر کنترل، پرسش‌ها، تصمیم‌ها و نقشهٔ خواندن؛ نه محل اجرای کد |

## ۷. ثبت شاخه‌ها

| شاخه | نقش | تصمیم فعلی |
|---|---|---|
| `main` | نسخهٔ خام پایه | نگه‌داری |
| `archive/original-2026-08-11` | Snapshot آرشیو خام | نگه‌داری |
| `agent/content-inventory-phase-1-2` | شاخهٔ کاریِ ثبت‌شده؛ PR شمارهٔ ۱ | مرجع کار فعلی |
| `archive/pre-rational-core-roadmap-2026-08-12` | Snapshot پیش از راهبرد | نگه‌داری |
| `archive/rational-core-roadmap-v1-2026-08-12` | Snapshot راهبرد | نگه‌داری |
| `archive/rational-core-v0.1-2026-08-12` | Snapshot هستهٔ ۰٫۱ | نگه‌داری |
| `archive/legacy-local-ssoc-2026-08-15` | Snapshot تاریخی | نگه‌داری تا ممیزی جدا |
| `agent/obsidian-knowledge-map` | شاخهٔ موقتِ نقشه؛ ایجادشده در این گفت‌وگو | کاندید حذف پس از تأیید |
| `agent/obsidian-raw-research-map` | شاخهٔ موقتِ نقشه؛ ایجادشده در این گفت‌وگو | کاندید حذف پس از تأیید |

## ۸. قانون ادامه

- بدون درخواست صریح، شاخهٔ جدید ساخته نمی‌شود.
- نقشه یا مستندات جایگزین نوت‌بوک و کد نیستند.
- هر ادعای جدید باید به فایل، Commit یا آزمون واقعی وصل باشد.
- هر بار فقط یک کارِ فعال داریم.
