# فهرست فایل‌ها

| فایل اصلی | ساختار | نقش واقعی محتوا | موضوع‌های غالب | ارزیابی فعلی | جایگاه پیشنهادی بعدی |
|---|---:|---|---|---|---|
| `1_roadmap.ipynb` | ۱۴ MD | یادداشت مسیر و تصمیم‌های پراکنده | هستهٔ عقلانی، منطق ارسطویی، مدل‌های مشابه | مولد برای تاریخچهٔ تصمیم؛ ناقص به‌عنوان Roadmap | مدیریت پژوهش / تاریخچهٔ تصمیم |
| `AI-Aristotle.ipynb` | ۲ MD | مقایسهٔ روایی منطق نمادین و شبکه عصبی | OpenCog، OpenNARS، منطق قطعی/احتمالی | مفید برای طرح پرسش؛ منابع ندارد | پژوهش منطق و کارهای مرتبط |
| `Essential_Motion.ipynb` | ۲۷ MD، فقط ۵ سلول غیرخالی | گفت‌وگوی فلسفی-علمی | خودآگاهی، حرکت جوهری، روح، IIT/GWT، زیست‌ماده، متاشناخت | الهام‌بخش اما عمدتاً غیرعملیاتی؛ چند ادعا نیازمند منبع | آرشیو فلسفه و فرضیه‌های دوربرد |
| `SSOC.ipynb` | ۷۵ MD + ۱ Code | دفتر اصلی گفتگو و گردآوری پاسخ‌ها | تعریف SSOC، معماری لایه‌ای، منابع، Roadmap، LLM | مهم‌ترین منبع تاریخی؛ بسیار تکراری و دارای منابع مشکوک | آرشیو مرکزی + استخراج ادعاها |
| `book.ipynb` | ۳ MD | فهرست مطالعه و خلاصهٔ چامسکی | زبان، فطرت، منابع شیعی و فلسفی | مسیر مطالعه است، نه نتیجهٔ پژوهش | پژوهش زبان و مبانی انسانی |
| `code.ipynb` | ۳ MD + ۶ Code | PoC لایه‌ای و دو نسخهٔ مینیمال | ابژه، رابطه، بازتاب، کنش | روابط ساختگی و hard-coded؛ بازتاب واقعی ندارد | آزمایش‌های اولیه / ناموفق ولی آموزنده |
| `core_of_Innate_knowledge.ipynb` | ۱۰ MD | جمع‌بندی بدیهیات و دانش فطری | ابهام زبانی، بدیهیات غربی/اسلامی، نقد شبکه عصبی | پرسش مرکزی ارزشمند؛ کیفیت منابع ناهمگون | پژوهش بدیهیات و فرضیه‌ها |
| `deep_think.ipynb` | ۲ MD | گفت‌وگوی نقدی درباره معنا و فهم | دیکشنری ذهنی، معنا، فطرت، اتصال‌گرایی | یکی از بخش‌های مولدتر چون محدودیت ایده را مطرح می‌کند | پژوهش زبان/معنا و پرسش‌های باز |
| `intelegence_core.ipynb` | ۴ MD + ۴ Code | طراحی هستهٔ منطق و parser فارسی | قیاس ارسطویی، گراف دانش، تناقض | نزدیک‌ترین بخش به مؤلفهٔ آزمون‌پذیر؛ ولی پیاده‌سازی ناقص | آزمایش مستقل منطق، پس از بازنویسی |
| `reflect.ipynb` | ۳۹ MD + ۱۰ Code | گفتگو و تکرارهای پیاده‌سازی بازتاب ارزش‌محور | بازتاب، قرآن، حرکت جوهری، مدل‌های مشابه، Hazm | بزرگ‌ترین کانون خلط مفهوم و کد؛ مسیر نسخه‌ها قابل‌ردگیری است | آرشیو آزمایش‌های بازتاب و شکست‌ها |
| `aristotle_logic.py` | ۱ فایل Python | نسخهٔ استخراج‌شدهٔ هستهٔ منطقی | parser فارسی، گراف، استنتاج، تناقض | از نظر syntax سالم؛ از نظر منطق و parser ناقص | مبنای یک آزمایش بازنویسی‌شده، نه کتابخانهٔ آماده |

## تکرارهای ثبت‌شده

- تکرار دقیق: `core_of_Innate_knowledge.ipynb:8` و `1_roadmap.ipynb:13`.
- تکرار نزدیک: `core_of_Innate_knowledge.ipynb:1` و `1_roadmap.ipynb:11`.
- نسخه‌های متوالی و بسیار مشابه کد: `reflect.ipynb:43`، `44` و `46`.
- `aristotle_logic.py` تقریباً همان کد `intelegence_core.ipynb:5` است.

تکرارها فعلاً حفظ شده‌اند، چون ترتیب تاریخیِ تغییر فکر را نشان می‌دهند.
